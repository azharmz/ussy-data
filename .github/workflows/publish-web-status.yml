name: Legacy web status publisher (retired)

# Retired: dashboard serving state is rebuilt into D1 by web/_worker.js
# from authoritative R2 universe/readiness/freshness sources.
# Kept temporarily as historical reference; no production trigger.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Retired. USSY Data Control is served through D1; web/status.json is no longer a production dependency."
